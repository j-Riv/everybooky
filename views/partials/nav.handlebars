<div>
    <nav class="navbar navbar-light navbar-expand-md navigation-clean-button">
        <div class="container">
            <a class="navbar-brand logo" href="/">Everybooky</a>
            <button class="navbar-toggler" data-toggle="collapse" data-target="#navcol-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="navbar-toggler-icon"></span>
            </button>
            <!-- Search Bar Start-->
            <form class="form-inline">
                <input type="text" class="form-control border-right-0 border-secondary" placeholder="Search" aria-label="Search Bar"
                    aria-describedby="searchBtn" id="searchBar">
                <div class="input-group-append">
                    <button class="btn border-left-0 border-secondary" type="button" id="searchBtn">
                        <i class="fas fa-search text-secondary"></i></button>
                </div>
            </form>
            <!-- Search Bar End -->
            <div class="collapse navbar-collapse" id="navcol-1">
                <ul class="nav navbar-nav mr-auto"></ul>
                <span class="navbar-text actions">
                    <a href="#" class="login" data-toggle="modal" data-target="#loginModalCenter">Log In</a>
                    <a class="btn btn-light action-button" role="button" href="{{signupLink}}"
                        style="background-color: rgb(2,164,211);">Sign Up</a>
                </span>
            </div>
        </div>
    </nav>
</div>